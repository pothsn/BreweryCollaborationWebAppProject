@model BreweryCollaborationWebbApp.Models.CollaborationRequest

@{
    ViewData["Title"] = "Create";

}

<h2>Create</h2>

<h4>CollaborationRequest</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SenderId" class="control-label"></label>
                <select asp-for="SenderId" class="form-control" asp-items="ViewBag.SenderId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SenderName" class="control-label"></label>
                <select asp-for="SenderName" class="form-control" asp-items="ViewBag.SenderName"></select>
            </div>
            <div class="form-group">
                <label asp-for="ReceiverId" class="control-label"></label>
                <select asp-for="ReceiverId" class="form-control" asp-items="ViewBag.ReceiverId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ReceiverName" class="control-label"></label>
                <select asp-for="ReceiverName" class="form-control" asp-items="ViewBag.ReceiverName"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
