@model BreweryCollaborationWebbApp.Models.BeerFanTaste

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<h4>BeerFanTaste</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FanId" class="control-label"></label>
                <select asp-for="FanId" class ="form-control" asp-items="ViewBag.FanId"></select>
            </div>
            <div class="form-group">
                <label asp-for="BeerStyleId" class="control-label"></label>
                <select asp-for="BeerStyleId" class ="form-control" asp-items="ViewBag.BeerStyleId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
